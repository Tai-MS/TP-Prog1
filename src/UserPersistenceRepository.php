<?php
use Doctrine\ORM\EntityRepository;
class UserPersistenceRepository extends EntityRepository
{
    // Por ahora, esta clase está vacía. La usaremos para extender
    // EntityRepository, en caso de que lo necesitemos.
}